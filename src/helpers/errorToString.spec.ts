import { describe, it } from "bun:test";

describe("errorToString", () => {
  describe("#errorToString", () => {
    it.todo("returns the error message when is a instance of Error");
    it.todo("returns error when is a string");
    it.todo("returns JSON stringfy when is an object");
  });
});
